language: python
python:
  - "3.8"
services:
- docker
cache: pip

matrix:
  fast_finish: true
  include:

    - name: "Test"
      stage: test
      install: true
      script: "./scripts/test.sh all"
